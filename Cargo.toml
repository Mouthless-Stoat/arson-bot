[package]
name = "arson_bot"
version = "0.1.0"
edition = "2021"

[dependencies]
poise = "0.6.1"
rand = "0.9.0"
tokio = { version = "1.44.2", features = [
  "macros",
  "rt-multi-thread",
] } # tokio for async shit cus it discord api
